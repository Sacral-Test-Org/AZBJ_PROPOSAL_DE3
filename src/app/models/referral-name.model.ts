export class ReferralName {
  branch_code: string;
  branch_name: string;

  constructor(branch_code: string, branch_name: string) {
    this.branch_code = branch_code;
    this.branch_name = branch_name;
  }
}